<%= form_with(model: check, local: true) do |form| %>
  <% if check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(check.errors.count, "error") %> prohibited this check from being saved:</h2>

      <ul>
      <% check.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hora_emtrada %>
    <%= form.datetime_select :hora_emtrada %>
  </div>

  <div class="field">
    <%= form.label :hora_salida %>
    <%= form.datetime_select :hora_salida %>
  </div>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
